databases:
  - name: stella-db
    plan: free
    databaseName: stella_ecommerce
    user: stella_user

services:
  - type: web
    name: stella-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_DIALECT
        value: postgres
      - key: DB_HOST
        fromDatabase:
          name: stella-db
          property: host
      - key: DB_USERNAME
        fromDatabase:
          name: stella-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: stella-db
          property: password
      - key: DB_DATABASE
        fromDatabase:
          name: stella-db
          property: database
      - key: DATABASE_URL
        fromDatabase:
          name: stella-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false

  - type: web
    name: stella-client
    runtime: static
    plan: free
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: build
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
